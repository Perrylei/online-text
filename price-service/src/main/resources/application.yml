server:
  port: 8084

spring:
  application:
    name: price-service
  # nacos启动服务发现
  cloud:
    nacos:
      discovery:
        # 应用服务 WEB 访问端口
        server-addr: 127.0.0.1:8848
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/price-service?characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: abc123